$ sqlite3 ex1
sqlite>create table tbl1(name varchar(200),actor varchar(200),actress varchar(200),director varchar(200),year of release  four smallint);
sqlite> name,actor,actress,director,year of release
sqlite> From actor
sqlite> Join name
sqlite> On name=actor
sqlite> select name
sqlite> from tbl1
